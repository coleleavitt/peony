/target
mold/
/tmp
rocq-tests/*.vo
rocq-tests/*.vok
rocq-tests/*.vos
rocq-tests/*.glob
rocq-tests/.*.aux

# research materials (large PDFs/HTMLs)
research/

# jfc / local tooling
jfc-transcript.md
.lia.cache
.jfc/
.codegraph/

# agent scaffolding: keep reusable project guidance visible even when a
# user-level global ignore hides agent folders by default; keep imported skills
# and transient plans local unless explicitly added later.
!.claude/
!.claude/**
!.junie/
.junie/plans/
.junie/skills/
!.junie/AGENTS.md
!.junie/agents/
!.junie/agents/**
!.junie/commands/
!.junie/commands/**
!.junie/rules/
!.junie/rules/**

# core dumps
core
core.*

# benchmark corpora inputs (large, reproducible via bench/capture.sh) + generated results
bench/corpora/*/inputs/
bench/results/
rust_out

